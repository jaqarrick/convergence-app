//default settings when client connects to new room

import AudioRackSettingsObject from "../../../types/AudioRackSettingsObject"
const audioRackSettings: AudioRackSettingsObject = {
	masterVolume: {
		name: "Master Volume",
		value: 0,
	},
}

export default audioRackSettings
